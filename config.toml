baseurl = "https://users.dcc.uchile.cl/~tvillega"
#baseurl = "/"
languageCode = "en-us"
# Add it only if you keep the theme in the `themes` directory.
# Remove it if you use the theme as a remote Hugo Module.
paginate = 5

[frontmatter]
  lastmod = ["lastmod"]

[params]
  # dir name of your main content (default is `content/posts`).
  # the list of set content will show up on your index page (baseurl).
  contentTypeName = "posts"

  # ["orange", "blue", "red", "green", "pink"]
  themeColor = "green"

  # if you set this to 0, only submenu trigger will be visible
  showMenuItems = 2

  # show selector to switch language
  showLanguageSelector = true

  # set theme to full screen width
  fullWidthTheme = false

  # center theme with default width
  centerTheme = false

  # if your resource directory contains an image called `cover.(jpg|png|webp)`,
  # then the file will be used as a cover automatically.
  # With this option you don't have to put the `cover` param in a front-matter.
  autoCover = true

  # set post to show the last updated
  # If you use git, you can set `enableGitInfo` to `true` and then post will automatically get the last updated
  enableGitInfo = false
  showLastUpdated = false

  # set a custom favicon (default is a `themeColor` square)
  # favicon = "favicon.ico"

  # Provide a string as a prefix for the last update date. By default, it looks like this: 2020-xx-xx [Updated: 2020-xx-xx] :: Author
  updatedDatePrefix = "Updated"

  # set all headings to their default size (depending on browser settings)
  # oneHeadingSize = true # default

  # whether to show a page's estimated reading time
  # readingTime = false # default
  readingTime = true

  # whether to show a table of contents
  # can be overridden in a page's front-matter
  # Toc = false # default

  # set title for the table of contents
  # can be overridden in a page's front-matter
  # TocTitle = "Table of Contents" # default

#[params.twitter]
  # set Twitter handles for Twitter cards
  # see https://developer.twitter.com/en/docs/tweets/optimize-with-cards/guides/getting-started#card-and-content-attribution
  # do not include @
#  creator = ""
#  site = ""

[languages]
  [languages.en]
    languageName = "English"
    title = "Tom's Website"
    subtitle = "Student of Computer Science at Universidad de Chile"
    owner = "Tom Villegas"
    keywords = ""
    copyright = ""
    menuMore = "Show more"
    readMore = "Read more"
    readOtherPosts = "Read other posts"
    newerPosts = "Newer posts"
    olderPosts = "Older posts"
    missingContentMessage = "Page not found..."
    missingBackButtonLabel = "Back to home page"
 
    [languages.en.params.logo]
      logoText = "Tom's Website"
      logoHomeLink = "https://users.dcc.uchile.cl/~tvillega"

    [languages.en.menu]
      [[languages.en.menu.main]]
        identifier = "about"
        name = "About"
        url = "/about"
        weight = 1
      [[languages.en.menu.main]]
        identifier = "skills"
        name = "Skills"
        url = "/skills"
        weight = 2
      [[languages.en.menu.main]]
        identifier = "projects"
        name = "Projects"
        url = "/projects"
        weight = 3

  [languages.es]
    languageName = "Español"
    contentDir = "content/es"
    title = "Sitio de Tomás"
    subtitle = "Estudiante de Ciencias de la Computación en la Universidad de Chile"
    owner = "Tomás Villegas"
    keywords = ""
    copyright = ""
    menuMore = "Mostrar más"
    readMore = "Leer más"
    readOtherPosts = "Leer otras publicaciones"
    newerPosts = "Publicaciones recientes"
    olderPosts = "Publicaciones antiguas"
    missingContentMessage = "Página no encontrada..."
    missingBackButtonLabel = "Regresar a la página de inicio"

    [languages.es.params.logo]
      logoText = "Sitio de Tomás"
      logoHomeLink = "https://users.dcc.uchile.cl/~tvillega/es"

    [languages.es.menu]
      [[languages.es.menu.main]]
        identifier = "acerca-de"
        name = "Acerca de"
        url = "/es/acerca-de"

[module]
  # In case you would like to make changes to the theme and keep it locally in you repository,
  # uncomment the line below (and correct the local path if necessary).
  # --
  # replacements = "github.com/panr/hugo-theme-terminal -> themes/terminal"
#[[module.imports]]
#  path = 'github.com/panr/hugo-theme-terminal'
